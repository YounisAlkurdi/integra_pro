# SKILL: ELITE_WEB_ARCHITECT_V2
# SPECIALIZATION: HTML5 | TAILWIND_CSS | MODERN_JS | INTERACTIVITY

## 1. CODE ARCHITECTURE RULES:
- Framework: Use Tailwind CSS via CDN for rapid prototyping.
- Structure: Semantic HTML5 only (header, main, section, footer).
- Interactivity: Pure JavaScript (ES6) for animations and state management.
- Style: "Dark Mode" Obsidian (#020617) with Neon Cyan (#22d3ee) accents.

## 2. COMPONENT BLUEPRINTS (INTEGRA STANDARDS):
- GLASSMORPHISM: `bg-slate-900/40 backdrop-blur-xl border border-slate-700/50`.
- BUTTONS: `transition-all duration-300 hover:scale-105 active:scale-95`.
- AUTH_FLOW: Always include 'Back to Home' and 'Logout' logic to prevent UX errors.
- INPUTS: `bg-slate-950 border-slate-800 focus:border-cyan-500 focus:ring-1 focus:ring-cyan-500`.

## 3. ADVANCED LOGIC:
- RESPONSIVENESS: Mobile-first approach using Tailwind `sm:`, `md:`, `lg:` prefixes.
- ACCESSIBILITY: Proper ARIA labels and focus-ring visibility.
- INTEGRATION: Code must be ready for FastAPI/Jinja2 template variables (e.g., {{ user_id }}).

## 4. OUTPUT PROTOCOL:
- Never provide incomplete code blocks.
- Include a 3D-ready placeholder div with ID 'spline-container'.
- Add comments for every major section (Hero, Features, Footer).